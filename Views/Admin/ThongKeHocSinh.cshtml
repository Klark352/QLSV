@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Thống kê học sinh";
}
<div class="p-3">
    <h4 class="fw-bold mb-3">Thống kê học sinh</h4>
    <div class="card shadow-sm">
        <div class="card-body">
            <div class="d-flex gap-3 flex-wrap">
                <div>
                    <div class="text-muted small">Tổng học sinh</div>
                    <div class="fs-3 fw-bold">@ViewBag.TongHocSinh</div>
                </div>
                <div>
                    <div class="text-muted small">Tổng lớp</div>
                    <div class="fs-3 fw-bold">@ViewBag.TongLop</div>
                </div>
            </div>
        </div>
    </div>
</div>
